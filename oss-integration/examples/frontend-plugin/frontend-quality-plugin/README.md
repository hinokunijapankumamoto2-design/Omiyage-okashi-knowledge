# frontend-quality-plugin

Goal-optimized plugin covering 11 selected capabilities for: Claude Codeで最高品質のWebサイトを作れるPluginを作って

Generated by `oss-integrate build`. Do not hand-edit: rerun the build instead,
so the provenance and the code stay in sync.

## Capabilities

- **Frontend Implementation** (`frontend-implementation`) — evidence VERIFIED, concept from fixture-org/frontend-craft-plugin
- **Visual Design** (`visual-design`) — evidence SUPPORTED, concept from fixture-org/frontend-craft-plugin
- **Responsive Design** (`responsive-design`) — evidence SUPPORTED, concept from fixture-org/frontend-craft-plugin
- **Browser Automation** (`browser-automation`) — evidence VERIFIED, concept from fixture-org/browser-qa-plugin
- **Screenshot Capture** (`screenshot-capture`) — evidence SUPPORTED, concept from fixture-org/browser-qa-plugin
- **Testing** (`testing`) — evidence SUPPORTED, concept from fixture-org/browser-qa-plugin
- **Accessibility Audit** (`accessibility-audit`) — evidence SUPPORTED, concept from GoogleChrome/lighthouse
- **Performance Audit** (`performance-audit`) — evidence SUPPORTED, concept from GoogleChrome/lighthouse
- **Image Comparison** (`image-comparison`) — evidence SUPPORTED, concept from americanexpress/jest-image-snapshot
- **Visual Regression Testing** (`visual-regression`) — evidence SUPPORTED, concept from americanexpress/jest-image-snapshot
- **Visual Review** (`visual-review`) — evidence INFERRED, concept from (combination)

## Architecture

### Orchestration

Route a request to the right capability, sequence verification after implementation, and degrade explicitly when a capability is unavailable.

Capabilities: (orchestration only)

### Build

Produce and modify the artifact the goal is about.

Capabilities: visual-design, frontend-implementation, responsive-design

### Verify

Turn claims about the artifact into measured facts.

Capabilities: browser-automation, screenshot-capture, testing, accessibility-audit, performance-audit, image-comparison, visual-regression

### Judge

Review the measured facts against the goal and report defects with evidence.

Capabilities: visual-review

### Evidence

Record, per capability, what was measured versus what was assumed, so the plugin output can be trusted or challenged.

Capabilities: (orchestration only)

## Workflow

- 1. Frontend Implementation (frontend-implementation)
- 2. Visual Design (visual-design)
- 3. Responsive Design (responsive-design)
- 4. Browser Automation (browser-automation)
- 5. Screenshot Capture (screenshot-capture)
- 6. Testing (testing)
- 7. Accessibility Audit (accessibility-audit)
- 8. Performance Audit (performance-audit)
- 9. Image Comparison (image-comparison)
- 10. Visual Regression Testing (visual-regression)
- 11. Visual Review (visual-review)

## Configuration

- `baseUrl` (string) — URL the verification capabilities run against. Required before any browser-backed check.
- `viewports` (string[]) — Viewport list used by responsive and screenshot capabilities, so every capability measures the same sizes.
- `evidenceStandard` (string) — Minimum evidence class a capability result must reach before it is reported as a fact.
- `failOn` (string) — Severity at which the verification workflow stops and reports failure.

## Known limitations

- Unresolved incompatible-dependency on `node`: Unresolved automatically: "node" is needed at incompatible majors by fixture-org/frontend-craft-plugin and fixture-org/browser-qa-plugin and GoogleChrome/lighthouse. A human must pin one major or drop a capability.

- `security-review`: UNKNOWN — Required capability "security-review" has no candidate in the seed registry or the supplied repositories. Recorded as UNKNOWN rather than substituted with something adjacent.

## Provenance

See [PROVENANCE.md](./PROVENANCE.md). Reused third-party code: **None**.
